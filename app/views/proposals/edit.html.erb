<%= link_to "Voltar para #{t(:proposal, scope: "activerecord.models")}", project_proposal_path(@project) %>
<h1>Editar <%= t(:proposal, scope: "activerecord.models") %></h1>

<%= render "form", object: [@project, @proposal] %>